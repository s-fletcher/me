import React from 'react';
import './index.scss';

class Logo extends React.Component {
    render () {
        return (
            <div className="Logo">
            </div>
        );
    }
}

export default Logo;